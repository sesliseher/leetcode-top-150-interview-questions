public class Solution {
    public int lengthOfLastWord(String s) {
        int length = 0;
        int i = s.length() - 1;
        
        // Skip trailing spaces
        while (i >= 0 && s.charAt(i) == ' ') {
            i--;
        }
        
        // Count characters of the last word
        while (i >= 0 && s.charAt(i) != ' ') {
            length++;
            i--;
        }
        
        return length;
    }
}


// This solution scans the string from the end to find the length of the last word.
// Steps:
// 1. Start from the end of the string.
// 2. Skip all trailing spaces (if there are any).
// 3. Count the number of characters until another space is found or the string ends.
// The count represents the length of the last word.
